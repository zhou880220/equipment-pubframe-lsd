<resources>

    <style name="AppTheme" parent="@style/Theme.Leanback" />

    <style name="AppThemeTwo" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/tv_progress</item>
        <item name="android:background">@mipmap/lsp_bg</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>